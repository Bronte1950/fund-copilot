// Reusable badge pill — provider label, doc type, confidence indicator
// TODO Phase 4: implement with colour variants (navy, gold, green, amber, red)
export default function Badge({ label, variant = 'default' }) {
  return null
}
