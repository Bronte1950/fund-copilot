// Phase 4: provider, doc_type, ISIN filter selects — used inside QueryBar
// TODO Phase 4: implement controlled select dropdowns
export default function FilterBar({ filters, onChange }) {
  return null
}
